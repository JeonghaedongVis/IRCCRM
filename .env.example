# Event CRM 환경변수 예시 — 실제 값을 .env 또는 셸에 export 해서 사용
# (.env 파일은 .gitignore에 포함되어 있음)

# ── BASIC 인증 ──
# 둘 다 설정되면 모든 /ui 및 /api 요청에 인증 강제
# 둘 중 하나라도 비어있으면 인증 비활성 (개발용)
CRM_USER=admin
CRM_PASS=change-me-please

# ── 서버 포트 ──
# 기본 8080. 다른 포트 쓰려면 변경
CRM_PORT=8080
